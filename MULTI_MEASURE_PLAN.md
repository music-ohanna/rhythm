# 4마디 창작 확장 설계안

## 현재 구현 상태

논문용 1단계 기능을 구현하였다.

- 1–4마디 집중 편집 탭
- 마디별 빈 마디·작성 중·완성 상태 표시
- 마디별 입력, 되돌리기, 선택 삭제, 현재 마디 전체 삭제
- 현재 마디 반복 재생
- 2·3·4마디 작품 길이 선택과 완성된 마디 전체 연속 재생
- 재생 마디 자동 화면 전환
- 다음 마디 축소 미리보기
- 공유 상태에 네 마디 저장 및 기존 1마디 링크 호환

스마트폰과 태블릿은 현재 구조가 확장 가능한 상태이지만, 실제 학습자용 배포 전 별도의 터치·화면 크기 사용성 검사가 필요하다. 스마트폰은 가로 화면을 우선 대상으로 하고, 태블릿은 가로·세로 화면을 모두 검증한다.

## 연구 목적과 설계 원칙

이 도구의 핵심 사용 흐름은 다음과 같다.

1. 학습자가 종이 악보에 리듬을 그린다.
2. 디지털 화면에 같은 리듬을 옮겨 실음으로 듣는다.
3. 들은 결과를 바탕으로 종이 악보를 수정한다.

따라서 여러 마디를 한 화면에 축소 배치하면 음표, V자 리듬, 정간보, 강약 표시의 가독성이 떨어진다. 권장안은 **4마디 작품을 만들되, 편집 화면에는 선택한 한 마디만 크게 보여 주는 방식**이다.

## 권장 화면 구조

- 상단 또는 악보 바로 위에 `1마디  2마디  3마디  4마디` 탭을 둔다.
- 현재 편집 중인 마디는 진한 색으로 표시한다.
- 각 탭에는 `비어 있음`, `작성 중`, `완성` 상태를 작은 점 또는 체크로 표시한다.
- 중앙 악보, V자 리듬, 정간보, 강약 표시는 현재처럼 화면 폭 전체를 사용한다.
- 재생 버튼은 `현재 마디 재생`과 `1–4마디 전체 재생`으로 나눈다.
- 전체 재생 중에는 활성 마디 탭이 자동으로 이동하고, 중앙에는 해당 마디가 크게 표시된다.
- 종이 활동지에도 1–4 번호를 붙여 디지털 마디 탭과 일대일로 대응시킨다.

## 최소 4마디 버전의 데이터 구조

현재 전역 `notes` 배열 한 개를 다음 구조로 확장한다.

```js
const score = {
  timeSignature: { top: 4, bottom: 4 },
  measures: [
    { id: 1, notes: [] },
    { id: 2, notes: [] },
    { id: 3, notes: [] },
    { id: 4, notes: [] }
  ]
};
let activeMeasureIndex = 0;
```

기존 그리기·입력·삭제·검사 함수는 `score.measures[activeMeasureIndex].notes`만 사용하게 한다. 각 음표의 `beatOffset`은 계속 마디 안의 상대 위치이므로 기존 한 마디 기보 로직을 대부분 유지할 수 있다.

## 전체 재생 방식

- 각 마디를 독립적으로 검사한 뒤 완성된 네 마디를 순서대로 예약한다.
- Web Audio의 동일한 절대 시간축에 네 마디를 한 번에 예약해 마디 경계의 밀림을 막는다.
- 메트로놈 강박은 각 마디의 시작에서 다시 강으로 재생한다.
- 비어 있거나 불완전한 마디가 있으면 해당 마디 번호를 직접 안내한다.
- 1마디 끝과 다음 마디 시작을 잇는 붙임줄은 1차 버전에서 제외하고 후속 기능으로 둔다.

## 단계적 구현 범위

### 1단계: 논문용 최소 기능

- 1마디/4마디 모드 선택
- 4개 마디 탭과 이전·다음 이동
- 마디별 입력, 수정, 삭제, 완성 검사
- 현재 마디 재생과 전체 4마디 재생
- 공유 상태에 네 마디 저장

### 2단계: 사용성 개선

- 마디 복사, 붙여넣기, 순서 바꾸기
- 작은 완성 상태 표시
- 전체 재생 중 자동 마디 전환

### 3단계: 8마디 이상

- 화면은 계속 한 마디 집중 편집을 유지한다.
- 마디 탭만 가로 스크롤 또는 4마디 단위 페이지로 확장한다.
- 16·32마디는 논문용 최소 버전 검증 뒤 후속 개발로 둔다.

## 전문가 의견 반영 문구 제안

> 창작 범위 확대 요구를 반영하여 최소 4마디 작품을 구성할 수 있는 단계적 확장안을 설계하였다. 다만 본 도구의 아날로그–디지털 왕복 창작 과정과 기보 가독성을 유지하기 위해 여러 마디를 한 화면에 축소 제시하지 않고, 선택한 한 마디를 크게 편집하면서 전체 4마디를 연속 재생하는 집중 편집 방식을 채택하였다.

## 검증 항목

- 종이 악보와 디지털 마디 번호의 대응을 학습자가 혼동하지 않는가
- 1마디에서 4마디로 확장해도 음표·V자·강약 표시 가독성이 유지되는가
- 전체 재생 중 마디 전환을 이해할 수 있는가
- 디지털 재생 후 종이 악보 수정이 실제로 일어나는가
- 4마디 과제가 인지 부담을 지나치게 높이지 않는가
